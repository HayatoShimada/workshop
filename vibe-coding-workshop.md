# vibe coding 研修 — 設計書から動くツールを作る

**題材**：パタンナーの設計工数記録ツール `worklog`（Windows / CLI + GUI）
**配布物**：本資料、`DESIGN.md`（設計書）

---

## 研修の狙い

### 当日のゴールは「完成」ではありません

2時間でこのツールを完成させることはできません。**目指すのは、続きを自分で作れる状態になることです。**

- 設計書がある
- git で戻せる
- 詰まったときの聞き方を知っている
- **ゼロから作る**のと**既にあるものを直す**のを、それぞれ1回ずつ通した

この4つが揃っていれば、残りは各自で進められます。研修中に完成させてしまうと、そのあと自力で拡張する経験が抜け落ちます。

### 特に身につけてほしいこと

**改造を繰り返せるようになること。** 業務ツールは、作った後のほうが長く付き合います。配布したら必ず「ボタンを増やして」「窓が邪魔」といった要望が来ます。ここを自分で直せるかどうかが、ツールが生き残るかどうかを決めます。

だから今日は、**ゼロから作るのは1回だけにして、改造を何度も繰り返します。**

---

## タイムテーブル

| 時間 | 内容 | 形式 |
|---|---|---|
| 0:00 - 0:10 | 完成形と設計書を読む | 座学 |
| 0:10 - 0:30 | CLI / VS Code / Claude Code | 実習 |
| 0:30 - 0:50 | plan・コンテキスト・skills・git | 実習 |
| 0:50 - 1:15 | **実習A**：watcher を作る（CLI・ゼロイチ） | 実習 |
| 1:15 - 1:45 | **実習B**：打刻ウィンドウを作って直す（GUI・改造） | 実習 |
| 1:45 - 2:00 | 持ち帰り課題と、詰まったときの進め方 | 座学 |

付録に、当日は扱いきれない内容（設計の考え方、残りの実装、配布手順）をまとめてあります。

---

## 事前準備

**当日インストールで時間が溶けるので、前日までに済ませてください。**

- [ ] Windows 10 (1809以降) または Windows 11
- [ ] Claude の有料プラン（Pro / Max / Team / Enterprise のいずれか）
  - **無料プランでは Claude Code は使えません**
- [ ] VS Code をインストール（https://code.visualstudio.com/）
- [ ] Git for Windows をインストール（https://git-scm.com/downloads/win）
- [ ] Python 3.12 をインストール（インストール時に「Add to PATH」にチェック）
- [ ] 会社PCの場合、ソフトのインストール許可を確認

---

# 第0部：これから作るもの（10分）

## 0-1. `worklog` とは

パタンナーが月末に記憶をたどって社内Excelに工数を入力している。この作業を **1日3〜5分に置き換える** ツールです。

```
[終日・自動]           [席を立つとき・1クリック]     [1日の終わり・3〜5分]    [月次]

PCの稼働を          離席打刻ウィンドウで            15分刻みのCSVを        転記用CSV
15分単位で記録  ＋   「休憩」「トワル組み」等を  ──▶  Excelで開き、空欄と  ──▶  を出力
                    クリックしてから離席            誤りだけ直して保存
```

## 0-2. 2つの部品と、その接点

このツールは **2つのプログラムが1組** で動きます。今日はこの両方を触ります。

```
┌──────────────────┐                          ┌──────────────────┐
│  打刻ウィンドウ    │ ──書く──▶ punch.csv ──読む──▶ │     watcher      │
│    （GUI）        │                          │     （CLI）       │
│                  │ ◀──読む── state.json ◀──書く── │                  │
└──────────────────┘                          └──────────────────┘
     実習B                                            実習A
```

**接点はファイル2つだけ、しかも一方向です。** 打刻ウィンドウは watcher の中身を知らないし、watcher も打刻ウィンドウの中身を知りません。

この分け方には理由があります。

| 効果 | 内容 |
|---|---|
| 片方を壊しても片方は動く | 別々のプログラムなので巻き込まれない |
| GUIの確認が速い | 打刻ウィンドウはWindows APIに触らないので、単体で表示確認できる |
| GUIなしでもCLIをテストできる | `punch.csv` を手で数行書けば動作確認できる |

**「部品を分けて、接点を絞る」というのは、今日一番重要な設計の考え方です。** これがないと、1か所直すたびに全体が壊れます。

## 0-3. 設計書 `DESIGN.md` を配ります

**今日はこの設計書をClaudeに渡して形にします。** 5分取るので、次の3か所だけ読んでください。

- **第3章 基本方式** … なぜこの作り方なのか
- **第4章 やること / やらないこと** … 特に「やらないこと」
- **第8章 ファイル仕様** … `state.json` と `punch.csv`

### 設計書と CLAUDE.md の役割分担

| ファイル | 中身 | 読まれるタイミング | 長さ |
|---|---|---|---|
| `DESIGN.md` | 仕様の本体。何を作るか | 必要なときだけ | 長くていい |
| `CLAUDE.md` | 前提と守ってほしいルール | **毎回自動で** | **短く** |

`CLAUDE.md` は毎回Claudeの作業机に載るので、長いほど作業スペースが減ります。**仕様は `DESIGN.md` に置き、`CLAUDE.md` からは参照するだけにします。**

`CLAUDE.md` は Claude に書かせて構いません（詳しくは付録A-7）。ただし「pandasを勝手に入れない」のような、**使ってみて分かったルールは手で足していく**部分なので、丸ごと再生成すると消えます。生成する部分と手書きの部分を分けておくのがコツです。

---

# 第1部：道具に慣れる（0:10 - 0:50）

**ここで学ぶことは、すべて設計書のどこかに紐づいています。** なぜ覚えるのかを併記します。

---

## 1-1. vibe coding とは（5分）

> **作りたいものを日本語で説明して、AI にコードを書かせる開発のやり方。**

`for` や `if` の書き方を覚えていなくても始められます。人間の仕事は、コードを打つことから **「何を作るか決める」「出てきたものが正しいか判断する」** に移ります。

| | 従来 | vibe coding |
|---|---|---|
| 主な作業 | コードを書く | 意図を説明する / 結果を確認する |
| 必要なスキル | 文法・ライブラリの知識 | 仕様を言語化する力 |
| 詰まる場所 | 「書き方が分からない」 | 「何が欲しいか自分でも曖昧」 |
| 時間配分 | 実装8割 / 設計2割 | 設計5割 / 確認4割 / 実装1割 |

### 構文は覚えなくていい。ただし読めたほうがいい

暗記は不要です。ただし「だいたい何をしているか雰囲気で追える」程度は今日中に身につきます。**読めないと判断できず、AIの言うことを信じるしかなくなります。** 分からないコードは遠慮なく聞いてください。

```
このファイルが何をしているか、プログラミング未経験者向けに5行で説明して
```

### 今日ずっと効いてくる心構え

1. **小さく作る** — 一度に全部作らせない。1機能ずつ、動かしてから次へ
2. **動かして確かめる** — 「できました」を信じない。必ず自分で実行する
3. **戻せるようにする** — git でこまめに記録。壊れたら戻す

---

## 1-2. Claude Code のインストール（8分）

Claude Code は、ターミナル（黒い画面）の中で動くAI開発ツールです。あなたのPCのファイルを読み、書き換え、コマンドを実行します。

### PowerShell か CMD かを見分ける

Windowsの黒い画面は2種類あり、打つコマンドが違います。

- 行頭が `PS C:\Users\あなたの名前>` → **PowerShell**
- 行頭が `C:\Users\あなたの名前>`（`PS` がない） → **CMD**

スタートメニューで「PowerShell」を検索して起動してください。**管理者として実行する必要はありません。**

### インストール

**PowerShell**

```powershell
irm https://claude.ai/install.ps1 | iex
```

**CMD**

```batch
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
```

| エラー | 原因 |
|---|---|
| `The token '&&' is not a valid statement separator` | PowerShell に CMD 用コマンドを貼っている |
| `'irm' is not recognized` | CMD に PowerShell 用コマンドを貼っている |

### インストール後

1. **ターミナルを一度閉じて開き直す**（PATH の反映に必要）
2. 確認

```powershell
claude --version
```

`2.1.211 (Claude Code)` のようにバージョンが出れば成功です。うまくいかないときは `claude doctor` で診断できます。

### ログイン

```powershell
claude
```

初回はブラウザが開くので、Claudeのアカウントでサインインします。

### 起動と終了

| 操作 | やること |
|---|---|
| `claude` | 起動（**作業したいフォルダの中で**打つ） |
| `/exit` | 終了 |
| `Esc` | Claude の作業を中断 |
| `Ctrl + C` | 強制中断 |

---

## 1-3. VS Code（5分）

### なぜエディタが要るのか

Claude Code だけでもアプリは作れます。それでも VS Code を使う理由は3つ。

1. **Claude が書いたコードを目で見る窓**になる
2. **どこが変わったか（差分）** が色付きで見える
3. **ターミナルがエディタの中に入っている**ので画面を行き来しなくていい

**今日は特に3番目が効きます。** GUIを改造するときは「コードを見る・実行する・ウィンドウを見る」を何度も往復するので、1画面にまとまっていると速度が違います。

### 今日覚える操作は4つ

| 操作 | ショートカット |
|---|---|
| フォルダを開く | `Ctrl + K` → `Ctrl + O` |
| ターミナルを開く | `Ctrl + @` |
| ファイル検索 | `Ctrl + P` |
| 保存 | `Ctrl + S` |

拡張機能から `Claude Code` を検索してインストールしておくと、差分がエディタ上で確認できます。日本語化は `Japanese Language Pack` で。

### 演習

1. デスクトップに `worklog` フォルダを作り、VS Code で開く
2. `Ctrl + @` でターミナルを開く
3. `claude` と打って起動 → `/exit` で抜ける

---

## 1-4. CLI に慣れる（7分）

### 一番大事な考え方：「今どこにいるか」

CLI には常に「現在地（カレントディレクトリ）」があります。エクスプローラーで今開いているフォルダと同じです。**命令はすべてこの現在地を基準に実行されます。**

**打ち間違えても、Enter を押すまでは何も起きません。** 怖がらずに打ってください。

### 覚えるコマンドは9個

| やりたいこと | コマンド |
|---|---|
| 今どこ？ | `pwd` |
| 中身を見る | `ls` |
| フォルダを移動 | `cd フォルダ名` |
| 一つ上に戻る | `cd ..` |
| フォルダを作る | `mkdir 名前` |
| ファイルの中身を見る | `cat ファイル名` |
| ファイルを消す | `rm ファイル名` |
| 実行中の処理を止める | `Ctrl + C` |
| 画面をきれいにする | `cls` |

**`cat` は今日よく使います。** `state.json` が正しく更新されているかを確認するのに使うためです。

### 手を動かさずに済ませる2つの技

- **Tab キー** … 名前を途中まで打って Tab で補完。打ち間違い防止に絶大な効果
- **↑ キー** … 前に打ったコマンドを呼び戻す

### パスの書き方

| 書き方 | 意味 |
|---|---|
| `C:\Users\taro\worklog` | 絶対パス |
| `src\main.py` | 相対パス（現在地から） |
| `.` | 現在地 |
| `..` | ひとつ上 |
| `~` | ホームフォルダ |

### 演習

```powershell
pwd                      # 今いる場所
cd ~\Desktop             # デスクトップへ
mkdir cli-practice       # フォルダを作る
cd cli-practice          # 中に入る
"hello" > memo.txt       # ファイルを作る
ls                       # 確認
cat memo.txt             # 中身を見る
cd ..                    # 一つ上へ
```

---

## 1-5. コンテキストウィンドウの管理（7分）

**vibe coding で一番つまずくポイントです。** 「さっきまで賢かったClaudeが急にポンコツになった」の原因はほぼこれ。

### コンテキストウィンドウ = Claude の作業机の広さ

Claude は会話中、次のものを全部机に並べています。

- あなたの指示と Claude の返答すべて
- 読み込んだファイルの中身
- 実行したコマンドとその出力
- `CLAUDE.md` やスキルの内容

机には広さの上限があり、**溢れそうになると古いものから要約されて片付けられます。** このとき最初に伝えた前提が消えることがあります。

### 症状

- 最初に決めたルールを守らなくなる
- さっき直したバグを作り直す
- 応答が遅くなる

### 対処

| コマンド | やること | 使う場面 |
|---|---|---|
| `/context` | 机の使用量を色付きで表示 | 定期的に確認 |
| `/compact` | 会話を要約して空きを作る | **同じ作業を続けたい**とき |
| `/clear` | 会話を捨てて新品に戻す | **別の作業に移る**とき |
| `/rewind` | 会話とコードを巻き戻す | おかしくなったとき |

### 今日の実習での使いどころ

**実習Aが終わったら `/clear` します。** watcher と打刻ウィンドウは別の部品なので、会話を持ち越す必要がありません。持ち越すとwatcherの試行錯誤が机に残ったまま、GUIの作業を始めることになります。

**GUI改造は特に机が埋まりやすい**ので注意してください。スクリーンショットを何枚も貼るためです。1課題ごとに `/compact` するくらいで丁度いいです。

### 10分ルール

**話が噛み合わなくなったら粘らないでください。** `/clear` して設計書から再開したほうが速い。これは技術ではなく判断の問題で、粘るほど時間を失います。

---

## 1-6. mode と skills（7分）

### mode（権限モード）

`Shift + Tab` を押すたびに切り替わります。画面下部に現在のモードが出ます。

| モード | 確認なしでやること | 使いどころ |
|---|---|---|
| **Manual** | ファイルを読むだけ | 全部自分で確認したいとき |
| **acceptEdits** | 読む + ファイル編集 | レビューしながら進める |
| **plan** | 読む + 調査。**編集はしない** | **未経験者に最強。まずここから** |
| **auto** | ほぼ全部（別のAIが安全チェック） | 長い作業 |

### 進め方の型

```
1. /plan で「こう作りたい」と相談する
     ↓ Claude が調べて計画を出す（コードはまだ書かれない）
2. 計画を読んで「そこは違う」を指摘する
     ↓
3. 承認する
     ↓
4. acceptEdits か auto で実装させる
     ↓
5. 自分で動かして確認 → git にコミット
```

**「いきなり作らせない。まず `/plan` で相談する」を口癖にしてください。**

今日は設計書があるので、`/plan` は「仕様を決める」ためではなく **「設計書のどこをどう実装するつもりか、先に見せてもらう」** ために使います。ここで方針違いに気づけば、実装をやり直さずに済みます。

### skills

同じ指示を毎回コピペしているなら、それは skill にすべきサインです。

**skill = 手順書を書いたファイル。`/名前` で呼び出せる。**

| 場所 | パス | 範囲 |
|---|---|---|
| 個人用 | `~/.claude/skills/<名前>/SKILL.md` | 自分の全プロジェクト |
| プロジェクト用 | `.claude/skills/<名前>/SKILL.md` | そのプロジェクトだけ（共有できる） |

```markdown
---
description: 打刻ウィンドウを起動して表示を確認する。GUIを直したあと、確認、動かして、と言われたら使う。
---

1. `python -m worklog punch` を起動する
2. ログファイル `logs/punch.log` にエラーが出ていないか確認する
3. エラーがあれば内容を報告し、なければ起動できた旨を伝える
```

**`description` が最重要です。** Claude はこの文章を見て「今これを使うべきか」を判断するので、**自分が実際に口にしそうな言葉**を入れてください。

### 最初から入っているスキル

| コマンド | 用途 |
|---|---|
| `/run` | **アプリを実際に起動して動かす。GUI改造で多用します** |
| `/code-review` | 変更をレビューして問題を指摘 |
| `/debug` | エラーの原因調査 |
| `/doctor` | 設定の健康診断 |

### CLAUDE.md と skill の使い分け

| | CLAUDE.md | skill |
|---|---|---|
| 読まれるタイミング | **毎回** | 呼ばれたときだけ |
| 向く内容 | 常に必要な**事実** | ときどき使う**手順** |
| 例 | 「Python 3.12を使う」 | 「GUI変更後の確認手順」 |

**長い手順を CLAUDE.md に書くと毎回机が狭くなります。手順は skill へ。**

---

## 1-7. git / GitHub（6分）

### なぜ必要か

Claude は一度に何十行も書き換えます。**「さっきまで動いていたのに壊れた。どこを変えたか分からない」が普通に起きます。**

git は「動いていた状態」を記録する仕組みです。壊れたら戻せます。**これがあるから大胆に試せる。vibe coding における git は保険ではなくアクセルです。**

### 今日、git の価値が一番わかる場面

**GUI改造です。** 見た目は好みの問題なので、「やっぱり前のほうが良かった」が実際に起きます。1課題ごとにコミットしておけば、いつでも前の見た目に戻せます。

### 覚える概念は3つ

| 用語 | 意味 | たとえ |
|---|---|---|
| **コミット** | 「今の状態」に名前をつけて記録 | ゲームのセーブ |
| **プッシュ** | 記録をクラウドに上げる | クラウドセーブ |
| **リポジトリ** | プロジェクト1つ分の記録の入れ物 | セーブデータのフォルダ |

**コマンドは覚えなくていいです。** 全部Claudeに日本語で頼めます。

```
git の初期設定をして、今の状態をコミットして
```
```
直前のコミットの状態に戻して
```
```
何を変更したか差分を見せて
```

### 回し方

```
1. 1機能できた   →  自分で動かして確認
2. 動いた！      →  すぐコミット  ← ここが重要
3. 次へ
4. 壊れた        →  /rewind か、直前のコミットに戻す
```

**「動いたらすぐコミット」が唯一のルールです。** 1時間分をまとめてコミットすると、戻る先が粗すぎて役に立ちません。

### 最初にやること

```
このフォルダを git リポジトリとして初期化して。
Python プロジェクト用の .gitignore を作って、
data/ と logs/ と .env は履歴に含めないようにして。
最初のコミットをして。
```

### 上げてはいけないもの

- パスワード、APIキー、トークン、`.env`
- **顧客名や品番を含むデータファイル**

`data/` を `.gitignore` に入れるのは、記録された品番が業務情報だからです。**一度上げたものは消しても履歴に残ります。**

---

# 第2部：実習A — watcher を作る（0:50 - 1:15）

**ゼロから作る回です。** 設計書の第8章を実装します。

## 2-1. つくるもの

| 出力 | 内容 |
|---|---|
| `data/raw/YYYY-MM-DD_window.csv` | アクティブウィンドウの記録（追記） |
| `data/state.json` | いまの状態（毎回上書き） |

`state.json` は**打刻ウィンドウが読むファイル**です。ここの形式を守らないと、実習Bで作るGUIが動きません。**部品どうしの接点は、勝手に変えてはいけない。** これが今日の設計上のポイントです。

## 2-2. 進め方

### ステップ1 — 計画を見せてもらう（5分）

```
/plan
DESIGN.md を読んでください。今日はまず watcher だけを作ります。
打刻ウィンドウは後で別に作るので、今は触らないでください。

作る範囲：
- アクティブウィンドウを 5秒ごとに取得
- 同じウィンドウが続く間はまとめ、切り替わったら window.csv に追記
- 操作の有無（アイドル判定）も記録
- state.json を毎回更新（DESIGN.md 8-2b の形式ちょうど）

実装に入る前に、どのファイルを作ってどう分けるつもりか教えてください。
```

**出てきた計画を必ず読んでください。** ファイルが1つにまとまっていたら分けるよう指示します。設計書の「1ファイル1役割」がここで効きます。

### ステップ2 — window.csv だけ作る（8分）

```
まず window.csv への記録だけを実装してください。
state.json はまだ作らなくていいです。

- Ctrl+C で安全に止まる
- 例外はログファイル logs/watcher.log に書く
- 必要なライブラリのインストール手順も教えて
```

**確認します。**

```powershell
python -m worklog watch
# メモ帳やブラウザを開いて30秒ほど待つ
# Ctrl+C で止める
cat data\raw\2026-08-30_window.csv
```

行が入っていれば成功。**ここでコミットします。**

```
動作確認できました。コミットしてください。
```

### ステップ3 — state.json を追加（8分）

```
次に state.json を追加してください。
DESIGN.md の 8-2b にある形式ちょうどで作ってください。
キーの名前を勝手に変えないでください。打刻ウィンドウがこれを読みます。
```

**確認します。** 別のターミナルを開いて、watcher を動かしたまま：

```powershell
cat data\state.json
```

`active` が `true` になっていることを確認したら、**キーボードとマウスを3分以上触らずに待って**もう一度：

```powershell
cat data\state.json
```

`active` が `false`、`idle_sec` が増えていれば成功です。

> **確認に3分かかるのが面倒な場合**：`config.toml` の `idle_threshold_sec` を一時的に `10` にしてください。**確認しやすいように設定を変えられるのは、設定を外に出しておいた効果です。**

コミットします。

### ステップ4 — 品番の抽出（4分）

```
config.toml の [extract] の正規表現でウィンドウタイトルから品番を抜き出し、
state.json の current_style と recent_styles に入れてください。
抽出できなければ空文字にして、エラーにはしないでください。
```

**抽出できなくても動くこと**が重要です。品番の命名規則はまだ未確認なので、ここが必須になると先に進めなくなります。

確認してコミットしたら、実習Aは終わりです。

## 2-3. 実習Aで詰まったときは

| 状況 | 打つ言葉 |
|---|---|
| エラーが出た | エラーメッセージを**全文そのままコピペ**して貼る |
| 原因が分からない | `直す前に、原因の仮説を3つ挙げて、それぞれの確かめ方を書いて` |
| 修正で余計に壊れた | `今の変更を全部やめて、直前のコミットに戻して` |
| 何をしているか分からない | `このファイルが何をしているか、未経験者向けに5行で説明して` |

**エラーは要約せず、そのまま貼るのが最強です。** 行番号やファイル名が原因特定の一番の手がかりになります。

---

## 実習Aと実習Bの間で `/clear` します

watcher と打刻ウィンドウは別の部品なので、会話を持ち越す必要がありません。持ち越すと、watcher の試行錯誤が机に残ったまま GUI の作業を始めることになります。

```
/clear
```

---

# 第3部：実習B — 打刻ウィンドウを作って直す（1:15 - 1:45）

**ここが今日の本番です。** ゼロから作るのは1回だけ。あとは改造を繰り返します。

## 3-1. なぜ改造を繰り返すのか

**GUIで難しいのは「作ること」であって「直すこと」ではありません。** ゼロから組むときはレイアウトの仕組みやイベントループを理解する必要がありますが、目の前に動くウィンドウがあって「ボタンをもっと大きく」「窓を右下に固定して」と言うだけなら、CLIより簡単なくらいです。しかも結果が目で見えるので確認が速い。

そして実務では、**配布後に一番よく直すことになるのがGUIです。** ボタンのラベル、窓の大きさ、位置、最前面にするかどうか。現場から必ず要望が来ます。ここを自分で直せることが、ツールを生かし続ける条件になります。

## 3-2. GUI作業で新しく覚える3つのこと

### (1) スクリーンショットを撮って渡す

**GUI作業で一番効く技です。** 「この余白が広すぎる」と文章で説明するより、撮って渡して「ここを詰めて」と言うほうが速く正確です。

```powershell
# Win + Shift + S で範囲を選んでコピー
# ペイントに貼って shot.png として保存
```

```
shot.png を見てください。ボタンの下の余白が広すぎます。半分にしてください。
```

VS Code の Claude Code 拡張機能を使っていれば、画像をそのまま添付できます。CLIから使う場合は、ファイルとして保存してパスを伝えるのが確実です。

**CLI作業では使わない技なので、ここで初めて出てきます。**

### (2) `/run` で実際に動かす

```
/run
```

Claude がアプリを起動して、変更が効いているか確認します。**GUIは「テストが通った」では確認になりません。** 実際に表示されて、押せて、期待通り動くところまで見る必要があります。

### (3) 見た目の指示は具体的に

「もっといい感じに」は通じません。

| ✕ | ◯ |
|---|---|
| もっといい感じに | 幅400px、高さ80px。ボタンは横並びで高さ40px |
| 小さくして | 幅を今の半分にして、フォントは10ptに |
| 邪魔にならないように | 常に最前面で、画面右下に固定して |

**設計書に「〜な感じで、を消す」と書いてあるのと同じ話が、目に見える形で効いてくる場面です。**

## 3-3. 事前に必ず言っておくこと

**tkinter は例外が黙って消えることがあります。** ボタンを押しても何も起きず、エラーも出ない、という状態になりやすい。

**最初のプロンプトの時点でこう指示してください。**

```
例外は握りつぶさず、必ず logs/punch.log に書き出してください。
ボタンのクリック処理も try/except で囲んで、失敗したらログに残してください。
```

これを忘れると、原因調査で長時間詰まります。

## 3-4. ゼロイチ（1回だけ・8分）

**設計書があるので、1つのプロンプトで出せます。**

```
/plan
DESIGN.md の第6章「離席打刻ウィンドウ」を読んでください。
これを Python の tkinter で作ります。

重要な制約：
- Windows API は一切使わないでください。純粋な tkinter だけで書いてください
- 離席の判定は data/state.json を読んで行ってください
- 打刻の結果は data/raw/YYYY-MM-DD_punch.csv に追記してください（8-2 の形式）
- 例外は握りつぶさず、必ず logs/punch.log に書いてください

今回は最小限でいいです。次の3つだけ実装してください：
- ボタン4つ（休憩 / トワル組み / 検品 / 打合せ）を横並びで表示
- クリックすると打刻を開始し、punch.csv に行が追記される
- もう一度クリックすると打刻を終了する

state.json の読み込み、品番の表示、自動終了はまだ実装しないでください。

実装前に、どう作るつもりか教えてください。
```

**確認します。**

```powershell
python -m worklog punch
```

ウィンドウが出て、ボタンを押して、`punch.csv` に行が入れば成功です。

```powershell
cat data\raw\2026-08-30_punch.csv
```

**コミットします。ここが改造の出発点になります。**

## 3-5. 改造（ここを厚く・20分）

**1課題ごとにコミットしてください。** 好みで行ったり来たりするので、戻れることが効きます。

### 改造1 — ボタンを設定ファイルから読む（5分）

```
ボタンの構成を config.toml の [[buttons]] から読むようにしてください。
label / process / counts_as_work / inherit_style の4項目です。
ボタンの数は設定次第で変わります。
```

**確認します。** `config.toml` にボタンを1つ足して、再起動してみてください。

```toml
[[buttons]]
label          = "サンプル確認"
process        = "検品・確認"
counts_as_work = true
inherit_style  = true
```

**ボタンが増えたら成功です。** これが「設定をコードから追い出す」の効果です。**現場から「ボタンを増やして」と言われたとき、プログラムを触らずに済みます。**

コミットします。

### 改造2 — 最前面表示と位置の記憶（5分）

```
以下を追加してください。
- 常に最前面に表示する
- 起動時、画面の右下に配置する
- ウィンドウを動かしたら、その位置を覚えて次回もそこに出す
- 位置は data/window_pos.json に保存する
```

**確認します。** 動かして、閉じて、もう一度開いて、同じ位置に出れば成功。

**ここでスクリーンショットを使ってみてください。** 出てきたウィンドウを撮って、

```
shot.png を見てください。ウィンドウが大きすぎて邪魔です。
高さを半分にして、ボタンの間の余白も詰めてください。
```

**何度か往復して、自分が使いたい見た目にしてください。** これが今日一番練習したい動きです。

コミットします。

### 改造3 — 品番プルダウン（5分）

```
ウィンドウの上部に品番のプルダウンを追加してください。
- data/state.json の recent_styles を選択肢にする
- 初期値は current_style
- 1秒ごとに state.json を読み直して、選択肢を更新する
- state.json が無い、または壊れている場合は「品番なし」として動き続けてください
  （エラーで落ちないこと）
- inherit_style が true のボタンは、この品番を punch.csv に書く
```

**最後の指示が重要です。** watcher を止めた状態でも打刻ウィンドウが動くことを確認してください。**片方が壊れても片方は動く**という設計を、実際に手で確かめる場面です。

```powershell
# watcher を止める
# state.json を消す
rm data\state.json
# 打刻ウィンドウを起動 → 落ちずに動けば成功
python -m worklog punch
```

コミットします。

### 改造4 — 打刻中の経過時間（5分）

```
打刻中のボタンに経過時間を表示してください。
「トワル組み (12分)」のように、1分ごとに更新してください。
打刻中のボタンは色を変えて、押されている状態が分かるようにしてください。
```

コミットします。

### 時間が余ったら

```
state.json の active が false から true に変わったら、
打刻中の記録を自動で終了してください。
ended_by には pc_resume と書いてください。
```

これで設計書どおりの動作になります。

## 3-6. 実習Bのまとめ

**今日やったのは、この繰り返しです。**

```
1. 何を変えたいか、具体的に書く
2. Claude に直させる
3. 動かして見る（必要ならスクリーンショットを撮って渡す）
4. 違ったら、また具体的に書く
5. 気に入ったらコミット
```

**このループが回せれば、残りの改造は全部同じやり方でできます。**

---

# 第4部：まとめと持ち帰り（1:45 - 2:00）

## 4-1. 今日の要点

1. vibe coding は「書く」より **「決める・確かめる」** 仕事になる
2. **`/plan` から始める。** いきなり作らせない
3. **1機能ずつ作り、動かして確認し、コミットする**
4. コンテキストは有限。**`/context` で見て、`/clear` で切る**
5. **設計書はファイルに書く。** 会話は消えるが、ファイルは残る
6. **部品を分けて、接点を絞る。** 片方が壊れても片方は動く
7. GUIは**作るより直すほうが簡単。** 改造の反復が本番
8. 見た目の指示は **具体的に。** スクリーンショットを渡す
9. **エラーは要約せずそのまま貼る**
10. **動いたらすぐコミット。** 壊れたら戻す

## 4-2. 持ち帰り課題

**設計書に全部書いてあるので、今日と同じやり方で進められます。**

| 順 | 課題 | 目安 |
|---|---|---|
| 1 | 打刻ウィンドウ：押し忘れの問いかけ（設計書 6章） | 1時間 |
| 2 | `worklog day`：タイムラインCSVの出力（設計書 8-3） | 2時間 |
| 3 | `worklog commit`：確定処理の取り込み | 1時間 |
| 4 | `worklog export`：転記用CSVの出力（設計書 8-4, 8-6） | 2時間 |
| 5 | `worklog status`：未確定日の一覧 | 30分 |
| 6 | exe化と配布（設計書 10章） | 2時間 |

**課題2が一番のヤマです。** `PC` / `打刻` / `休憩` / `未記録` の4状態を、優先順位どおりに埋める処理になります。ここも `/plan` から始めてください。

### 進めるときのコツ

- **1課題ごとに `/clear` する。** 別の機能なので会話を持ち越さない
- **毎回 `DESIGN.md を読んでください` から始める**
- **動いたらコミット**
- **詰まったら10分で切り上げて `/clear`**

## 4-3. 詰まったときの進め方

| 状況 | 対処 |
|---|---|
| エラーが出た | メッセージを**全文そのまま**貼る |
| 原因が分からない | `直す前に、原因の仮説を3つ挙げて、確かめる方法を書いて` |
| 修正で余計に壊れた | `今の変更を全部やめて、直前のコミットに戻して` |
| 話が噛み合わない | `/clear` → `DESIGN.md を読んで、実装済みと未実装を一覧にして` |
| 勝手に機能が増える | `DESIGN.md` の「やらないこと」に追記してから再依頼 |
| ライブラリを増やされた | `標準ライブラリだけで書き直せますか。無理ならなぜか説明して` |
| GUIが反応しない | `logs/punch.log` を見る。何も出ていなければ例外処理が抜けている |

## 4-4. その先

| 週 | やること |
|---|---|
| 1週目 | 持ち帰り課題1〜3。自分ひとりで1日使ってみる |
| 2週目 | 課題4〜5。パタンナー1人に試してもらう |
| 3週目 | 出てきた要望をGUI改造で反映する |
| 4週目 | exe化して配布 |

**3週目が一番大事です。** 要望に自分で応えられるかどうかが、このツールが定着するかを決めます。今日練習した改造のループが、そのまま使えます。

---

# 付録A：設計の考え方

**当日は詳しく触れませんが、次に自分で何かを作るときに必要になる内容です。**

## A-1. アプリにはいろんな形式がある

| 形式 | 作りやすさ | 配布のしやすさ | 例 |
|---|---|---|---|
| **CLI** | ◎ 一番簡単 | △ 相手もCLIが要る | `worklog export` |
| **デスクトップGUI** | △ | △ インストーラが要る | 打刻ウィンドウ |
| **Web** | ○ | ◎ URLを送るだけ | 社内の申請システム |
| **スクリプト** | ◎ | ○ | 定時バッチ |

**迷ったら CLI + ファイル出力を選んでください。**

1. 作る量が圧倒的に少ない（画面がないので）
2. 自動実行しやすい（タスクスケジューラで定時実行できる）
3. 出力をCSVやHTMLにすれば、見た目はExcelやブラウザに任せられる

### `worklog` でこの判断をどう使ったか

**日次の確定作業をExcelでやることにして、そのための画面を作りませんでした。** CSVを出力して、パタンナーが使い慣れたExcelで開いてもらう。これで確定画面をまるごと作らずに済んでいます。

一方で **離席打刻だけはGUIにしました。** 席を立つときに1クリックで押せる必要があり、これはCLIでは代替できないためです。

**「画面を作る/作らない」は機能ごとに判断します。** 全部GUIにする必要も、全部CLIにする必要もありません。

## A-2. フロントエンドとバックエンド

```
┌─────────────────────┐        ┌──────────────────────────┐
│    フロントエンド      │  ───▶  │      バックエンド          │
│  ・見た目            │        │  ・計算、集計             │
│  ・ボタン、入力欄      │  ◀───  │  ・データの保存、読み出し   │
│  ・「人が触る側」      │        │  ・「人が触らない側」       │
└─────────────────────┘        └──────────────────────────┘
```

**一人用のツールなら、この2つを分ける必要はありません。** 分けるのは「たくさんの人が同時に使う」「スマホからもPCからも使う」といった事情があるときだけです。

ただし `worklog` では、**別の理由で部品を分けています。**

| 分ける理由 | `worklog` の場合 |
|---|---|
| 多人数が同時に使う | 該当しない |
| **片方が壊れても片方を動かしたい** | **該当する** |
| **片方だけ単体でテストしたい** | **該当する** |

打刻ウィンドウと watcher を分けたのは、フロント/バックの原則からではなく、**壊れ方を局所化するため**です。分ける理由は毎回違います。「分けるのが正しいから分ける」ではなく、**何のために分けるのかを言えるようにしてください。**

## A-3. 誰が使うかが一番重要

同じ「工数集計ツール」でも、使う人が変わると別物になります。

| 項目 | 自分ひとり | チーム |
|---|---|---|
| データの置き場所 | PC内のCSV | 共有DB / 共有ドライブ |
| ログイン | 不要 | 必要 |
| エラー時 | 落ちてもいい | 落ちてはいけない |
| 設定の変更 | ファイルを直接編集 | 設定画面が要る |
| ドキュメント | 不要 | 必須 |
| 想定作業量 | 1〜2時間 | 数週間 |

差は10倍以上です。

### `worklog` は中間を狙っています

チームに配りますが、**「一人用ツールを人数分配る」**という形です。

- データは各自のPC内（共有しない）
- ログインなし
- 設定は `config.toml` を直接編集（設定画面を作らない）
- ただし **配布のためexe化は必要**、**設定を担当別に配り分ける** 仕組みは要る

**「チームで使う」を全部真に受けると数週間かかります。** どの要素が本当に必要かを個別に判断してください。

## A-4. プログラムがやること / 人がやること

**vibe coding で一番よくある失敗が「全部自動化しようとする」です。**

| 機械が得意 | 人間が得意 |
|---|---|
| 決まった手順の繰り返し | 例外的な判断 |
| 大量データの集計 | 曖昧なものの分類 |
| 正確な記録 | 「これは違う」という違和感 |
| 24時間動き続ける | 文脈を踏まえた解釈 |

### `worklog` での線引き

| 作業 | 担当 | 理由 |
|---|---|---|
| どのアプリが何分アクティブだったか | **機械** | 人には不可能 |
| 品番の抽出 | **機械（ルール）** | 命名規則があれば当たる |
| **PC外で何をしていたか** | **人（1クリック）** | 機械には知りようがない |
| 工程（初回パターンか修正か） | **人** | タイトルからは判定不能 |
| 集計と整形 | **機械** | 定型 |

**狙うのは「9割自動 + 1割人手」です。** 10割自動を目指すと、作業量が跳ね上がるうえに精度が落ちます。

> **アンチパターン**：「ウィンドウタイトルを毎回AIに投げて、どのプロジェクトか判定させる」
> → 遅い・お金がかかる・毎回結果が変わる。**ルールで9割やって、残りを人が30秒で片付ける**ほうが速くて確実です。

### PC外作業を捨てない

**パタンナーの業務は約半分がPC外です。** 一般的な作業記録ツールは離席を「非作業」として捨てますが、それをやると工数が実感より明らかに少なく出ます。**一度そうなるとツールは信用されません。**

離席打刻を入れたのは、この半分を拾うためです。**「機械が取れない部分をどう人に埋めてもらうか」を設計したことになります。**

## A-5. 保守しやすいプログラム設計

「保守しやすい」の定義が、vibe coding では従来と少し違います。

> **Claude が読んで、5分で全体を理解できる構造になっていること。**

理解しやすい構造なら、10回目の修正でも1回目と同じ精度で直せます。理解しにくいと、修正のたびに壊れます。

### 守るべき6つ

**1. 1ファイル1役割、小さく分ける**

```
✕  main.py（800行に全部入り）
◯  watcher.py       記録する
   state.py         state.json を書く
   punch_window.py  打刻ウィンドウ
   timeline.py      タイムラインを作る
   export.py        転記用CSVを出す
```

「レポートの書式を直したい」ときに1ファイル読めば済む。**Claude が読む範囲が狭いほど、正確で速く、コンテキストも節約できます。**

**2. 設定をコードから追い出す**

ボタン構成、勤務時間、品番の抽出ルール、出力の列名。全部 `config.toml` と `export_map.toml` に出しています。**設定変更のたびにプログラムを触る必要がなくなります。**

実習Bの改造1で、ボタンを設定から読むようにしたのはこのためです。**現場から「ボタンを増やして」と言われたとき、tomlを3行足すだけで済みます。**

**3. データはプレーンテキスト（CSV / JSON）で持つ**

データベースは便利ですが、壊れたときに中身が見えません。CSVならExcelでもメモ帳でも開けて、目視で直せます。**一人用ツールでは、これが圧倒的に強い。**

**4. 「生データ」と「加工結果」を分ける**

```
data/raw/     ← 記録したそのまま。絶対に上書きしない
data/daily/   ← 本人が確定したもの
data/export/  ← 集計結果。何度作り直してもいい
```

品番の抽出ルールを後から足したとき、生データが残っていれば過去分もやり直せます。**上書きしてしまうと、二度と戻せません。**

**5. ログを吐く**

**特にGUIでは必須です。** tkinter は例外が黙って消えることがあり、ログがないと「押しても何も起きない、エラーも出ない」という調査不能な状態になります。

**6. 依存ライブラリを増やさない**

便利そうなライブラリを次々足すと、環境が壊れたときに直せなくなります。**exe化して配布する場合、依存が多いほどファイルが大きくなり、トラブルも増えます。**

### 部品の接点を絞る

これが `worklog` で一番強く効いている判断です。

```
打刻ウィンドウ  ──▶ punch.csv ──▶  watcher / 本体
       ◀── state.json ◀──
```

**ファイル2つだけ、しかも一方向。** 打刻ウィンドウは watcher の関数を呼ばないし、その逆もありません。

この結果として、

- 打刻ウィンドウはWindows APIに触らずに済み、単体で動作確認できる
- watcher が落ちても打刻できる
- `punch.csv` を手で書けば、GUIなしでタイムライン生成をテストできる

**接点を決めたら、勝手に変えないでください。** 「ちょっと便利だから」とキー名を変えると、もう片方が動かなくなります。

## A-6. 設計書の書き方

### なぜ書くのか

1. **コンテキストが消えても再開できる** — 会話は消えるが、ファイルは残る
2. **Claudeへの一次資料になる** — 「仕様は `DESIGN.md` を見て」で済む
3. **自分の考えが整理される** — 書けないものは、作れません

### テンプレート

```markdown
# ◯◯ 設計書

## 1. 何を解決するか
（1文。成功の定義も書く）

## 2. 前提と制約
（分かっていないことも「未確認」と書く）

## 3. 基本方式
（どういう作り方をするか。なぜそれを選んだかも）

## 4. やること / やらないこと
（「やらないこと」が仕様の半分）

## 5. データ構造
（どのフォルダに、どんなファイル名で、どんな中身か）

## 6. 画面 / コマンド仕様

## 7. ファイル仕様
（具体的なサンプルを手で書く）

## 8. 配布と運用

## 9. 完成の判定条件

## 10. 確認したいこと
（分かっていないことの一覧）
```

### コツ

**「〜な感じで」を全部消す**

| ✕ | ◯ |
|---|---|
| いい感じにレポートを出す | プロジェクト別の合計時間を、多い順に表示する |
| こまめに記録する | 5秒ごとに記録する |
| 見やすく表示する | 0.25時間単位に丸めて、`- 案件A: 3.5h` の形式で |

**出力サンプルを手で書く — これが一番効きます**

文章で説明を尽くすより、欲しい出力を1つ手で書いたほうが正確に伝わります。`DESIGN.md` の 8-1 〜 8-4 が全部これです。

**「やらないこと」を書く**

書かないと、Claudeは良かれと思って余計な機能を足します。

**「未確認」も書く**

`DESIGN.md` では「品番がファイル名に入っているか未確認」と明記し、**未確認でも動く設計**にしています。分からないことを書いておくと、それを避ける設計になります。

**検討して却下した案も書く**

「打刻のみ」「日次確定のみ」を却下した理由を書いてあります。**後から「なぜこうしたんだっけ」となったとき、これがないと同じ議論をやり直すことになります。**

### Claudeに書かせる

ゼロから自分で書かなくて構いません。

```
/plan
◯◯を作りたい。（ざっくり説明）
まず設計書を一緒に作りたい。
いきなり書き始めずに、仕様を固めるための質問を10個して。
```

**答えづらい質問こそ、あなたが決めていなかったことです。** 答えてから書かせ、出てきたものを必ず読んで直してください。

## A-7. DESIGN.md と CLAUDE.md の分け方

| | `DESIGN.md` | `CLAUDE.md` |
|---|---|---|
| 中身 | 仕様の本体 | 前提とルール |
| 読まれる | 必要なときだけ | **毎回自動で** |
| 長さ | 長くていい | **短く** |
| 書く人 | 人（Claudeと対話しながら） | Claudeに書かせてよい |

`CLAUDE.md` は毎回作業机に載るので、長いほどスペースが減ります。**仕様は `DESIGN.md` に置き、`CLAUDE.md` からは参照するだけにします。**

### CLAUDE.md を生成するときの注意

**丸ごと再生成すると、手で足したルールが消えます。**

`CLAUDE.md` で本当に価値があるのは、設計書から導けない部分です。

```
- 新しいライブラリを追加する前に必ず確認を取ること
- data/ 配下のファイルを直接編集しないこと
- state.json のキー名を変えないこと（打刻ウィンドウが読んでいる）
- tkinter の例外は必ず logs/ に書くこと
```

**これらは「Claudeが実際にやらかしたのを見て後から足したルール」です。** 設計から演繹できません。

対策は2つ。

1. 生成部分をマーカーで囲み、そこだけ差し替える
2. **そもそも生成部分を極限まで削る** — 「仕様は `DESIGN.md`。挙動を変える前に必ず読むこと」の2行で足ります

**2番目のほうがシンプルで事故りにくいです。** 設計書があるなら、`CLAUDE.md` に仕様を要約して置く意味はあまりありません。

### 運用ルール

- **`DESIGN.md` を変えたら `CLAUDE.md` を見直す**
- **`CLAUDE.md` は git にコミットする** — 差分が「設計がどれだけ動いたか」の指標になる

---

# 付録B：残りの実装

**すべて `DESIGN.md` に仕様が書いてあります。**

## B-1. `worklog day`（タイムライン出力）

**一番のヤマです。** 4状態を優先順位どおりに埋めます。

**優先順位：打刻 ＞ 休憩設定 ＞ PC操作 ＞ 未記録**

| 状態 | 判定 | 事前入力 |
|---|---|---|
| `打刻` | `punch.csv` にその時間帯の打刻がある | 打刻内容をそのまま |
| `休憩` | `config.toml` の休憩時間帯 | 空欄（集計対象外） |
| `PC` | `window.csv` に操作記録がある | ヒントから推定 |
| `未記録` | どれにも当たらない | **空欄。本人が埋める** |

打刻をPC操作より優先するのは、**打合せ中にノートPCを触っている**ようなケースを想定しています。人が明示的に宣言した情報のほうが、推測より確実です。

```
/plan
DESIGN.md の 8-3 を読んでください。worklog day を実装します。
punch.csv と window.csv から、15分刻みのタイムラインCSVを作ります。
状態の優先順位は 打刻 > 休憩設定 > PC操作 > 未記録 です。
実装前に、どう作るつもりか教えてください。
```

**テストにGUIは要りません。** `punch.csv` を手で数行書けば確認できます。

## B-2. `worklog commit`（確定処理）

編集済みタイムラインを読み込んで確定します。

- 編集前と比べて、品番・工程が埋まっているか検証する
- `未記録` が残っていたら警告する
- 確定済みの日を記録する

## B-3. `worklog export`（転記用CSV）

**`export_map.toml` で列を定義できることが必須要件です。**

社内Excelの列構成はまだ未確認なので、**現物が来たときに設定ファイルだけで対応できる**ようにしておきます。ここをコードに埋め込むと、列が1つ変わるたびにプログラムを直すことになります。

- `counts_as_work = false` の工程（休憩など）は出力しない
- 時間は0.25h単位
- Excelで開くため UTF-8 BOM 付き

## B-4. exe化と配布

```
PyInstaller で単一の exe にまとめてください。
config.toml と export_map.toml は exe と同じフォルダに置く形にしてください。
watcher と 打刻ウィンドウは別々の exe にしてください。
```

- スタートアップフォルダにショートカットを置いて自動起動
- 日々の操作はバッチファイルから（パタンナーがコマンドを打たなくて済むように）
- 更新は共有フォルダのexeを差し替える

---

# 付録C：配布時に必ず説明すること

**これは技術ではなく運用の話ですが、省略すると失敗します。**

1. **何を記録するか** … PCで開いているウィンドウのタイトル、操作の有無、押した打刻
2. **どこに保存されるか** … 自分のPCの中だけ。生ログも打刻ログも外に出ない
3. **何が提出されるか** … 品番×工程×時間 の集計値のみ。**休憩の記録は含まれない**
4. **止められること** … いつでも記録を停止でき、確定前の内容は自由に編集できる
5. **使わない用途** … 勤怠管理、人事評価には使用しない

**特に休憩ボタンがあることで勤怠監視と受け取られやすいので、提出データに含まれない点を明示してください。**

設計上、`counts_as_work = false` の工程は集計から除外されます。**データが構造的に出ていかないことを設計で担保しているほうが、口頭の説明より強いです。**

導入前に上長・人事への確認も行ってください。他人のPCの稼働を記録する仕組みは、説明の仕方によっては労務上の問題になります。

---

# 付録D：チートシート

## Claude Code

| コマンド | 用途 |
|---|---|
| `/plan` | 計画モード（編集せず提案だけ） |
| `/context` | 作業机の使用量 |
| `/clear` | 会話をリセット（別タスクへ） |
| `/compact` | 会話を要約して空きを作る |
| `/rewind` | 会話とコードを巻き戻す |
| `/run` | **アプリを起動して動作確認** |
| `/code-review` | 変更をレビュー |
| `/diff` | 変更点を見る |
| `/init` | CLAUDE.md を生成 |
| `/memory` | CLAUDE.md を編集 |
| `/doctor` | 設定の健康診断 |
| `/resume` | 過去の会話を再開 |
| `/exit` | 終了 |

## キー操作

| キー | 動作 |
|---|---|
| `Shift + Tab` | モード切替 |
| `Esc` | 作業を中断 |
| `Ctrl + C` | 強制中断 |
| `↑` | 前のコマンド |
| `Tab` | 名前の補完 |
| `Win + Shift + S` | **スクリーンショット（範囲選択）** |

## PowerShell

| コマンド | 用途 |
|---|---|
| `pwd` | 現在地 |
| `ls` | 中身一覧 |
| `cd 名前` / `cd ..` | 移動 |
| `mkdir 名前` | フォルダ作成 |
| `cat ファイル` | 中身を表示 |
| `rm ファイル` | 削除 |
| `cls` | 画面クリア |

## よく使うプロンプト

```
DESIGN.md を読んでください。
```
```
実装前に、どう作るつもりか教えてください。
```
```
直す前に、原因の仮説を3つ挙げて、それぞれの確かめ方を書いて
```
```
今の変更を全部やめて、直前のコミットに戻して
```
```
このファイルが何をしているか、未経験者向けに5行で説明して
```
```
shot.png を見てください。（具体的な指示）
```

---

# 付録E：困ったとき

| 症状 | 対処 |
|---|---|
| `claude` が見つからない | ターミナルを閉じて開き直す → `claude doctor` |
| `The token '&&' is not...` | PowerShell に CMD 用コマンドを貼っている |
| `'irm' is not recognized` | CMD に PowerShell 用コマンドを貼っている |
| ログインできない | 無料プランでは使えません。Pro以上が必要 |
| Claudeが指示を忘れる | `/context` を確認 → `/clear` して設計書から再開 |
| 同じバグを作り直す | `/clear` してから、対象ファイルだけ指定して依頼 |
| 勝手に機能が増える | `DESIGN.md` の「やらないこと」に追記して再依頼 |
| 全部壊れた | `/rewind`、または `直前のコミットに戻して` |
| **GUIが表示されない** | `logs/punch.log` を確認 |
| **ボタンを押しても何も起きない** | 例外が握りつぶされている。`ボタンの処理を try/except で囲んでログに書いて` |
| **ウィンドウが最前面にならない** | tkinter の `attributes('-topmost', True)` が必要 |
| **state.json が読めずGUIが落ちる** | `state.json が無くても動くようにして` と指示 |

## 公式ドキュメント

- Claude Code：https://code.claude.com/docs/en/overview
- インストール：https://code.claude.com/docs/en/setup
- 権限モード：https://code.claude.com/docs/en/permission-modes
- スキル：https://code.claude.com/docs/en/skills
