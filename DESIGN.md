# worklog 設計書

パタンナーの設計工数記録ツール

| | |
|---|---|
| 版 | 0.2（研修用ドラフト） |
| 対象OS | Windows 10 (1809+) / Windows 11 |
| 配布先 | パタンナーチーム |
| 今回のスコープ | 転記可能なCSVの出力まで |

---

## 1. 何を解決するか

パタンナーが月末に記憶をたどって社内サーバーのExcelに工数を入力している。この入力作業の時間と、思い出しによる精度の低さを解消する。

**成功の定義**

- 工数入力にかかる時間が、月末まとめて数十分 → 1日3〜5分に置き換わる
- 入力される工数が「思い出し」から「その日の記録」になり、精度が上がる

**この2つを満たさないなら、機能が増えても失敗です。**

---

## 2. 前提と制約

| 項目 | 内容 |
|---|---|
| PC前作業の比率 | 業務時間の**約半分**。残りはトワル組み・検品・打合せ等 |
| 品番の自動取得 | ファイル名に品番が入っているか**未確認**。自動抽出に依存しない設計とする |
| 記録範囲の方針 | 全アプリのウィンドウタイトルを記録してよい（現時点の合意） |
| 使う人 | パタンナー（非エンジニア）。Excelは日常的に使う |
| 配布 | 各自のPCにインストール。インストーラなし |
| 転記先 | 社内サーバーのExcel。**列構成は未確認** |

---

## 3. 基本方式：離席打刻 ＋ 日次タイムライン確定

### 全体の流れ

```
[終日・自動]              [席を立つとき・1クリック]        [1日の終わり・3〜5分]      [月次]

PCの稼働状況を            離席打刻ウィンドウで              15分刻みのタイムラインを     確定分を
15分単位で記録    ＋      「休憩」「トワル組み」等を   ──▶  Excelで開き、空欄と    ──▶  集計して
・アクティブなアプリ         クリックしてから離席              誤りだけを直して保存        転記用CSV
・ウィンドウタイトル         （戻りは自動判定）
・操作の有無
```

**PC作業は自動で、PC外作業は離席時の1クリックで拾い、残った穴だけを1日の終わりに埋める。** これが基本の考え方です。

### なぜこの方式か

**PC外作業が半分あるため、ウィンドウ監視だけでは工数の半分しか取れない。** 残りを月末に思い出して入力するなら省力化になりません。離席打刻でその半分を直接拾います。

**打刻は「離れるとき」の1回だけにする。** 席を立つ瞬間はまだPCの前にいるので押せます。復帰はPC操作の検知で自動終了させるため、戻ってきたときに押す必要はありません。押す回数が増えるほど守られなくなるので、**1離席につき1クリックを上限とします。**

**それでも押し忘れる分は、日次確定で拾う。** 打刻も自動記録もない時間帯はタイムライン上で空欄になるので、そこだけ埋めれば済みます。

**品番の自動取得可否が未確認でも着手できる。** 自動抽出はヒント列の精度にしか影響しません。命名規則が判明した時点でルールを追加すれば、設計を変えずに自動化率だけを上げられます。

### 検討したが採用しなかった方式

| 方式 | 判断 |
|---|---|
| 打刻のみ（日次確定なし） | 不採用。押し忘れた時間が丸ごと欠落し、あとで思い出す作業に戻る |
| 日次確定のみ（打刻なし） | 不採用。PC外の半分を全部記憶に頼ることになり、確定作業が長くなる |
| 常駐トレイアイコンのメニューから打刻 | 不採用。右クリック→メニュー展開→選択で3操作かかる。常時表示の小窓で1クリックにする |
| 全自動分類（タイトルから品番・工程を推定） | 不採用。工程はタイトルから判定不能。品番も命名規則が未確認 |

---

## 4. やること / やらないこと

### やること

- PC操作の記録（アクティブウィンドウ、プロセス名、操作の有無）
- 離席打刻ウィンドウ（ボタン構成は設定ファイルで定義）
- 勤務時間・休憩時間の設定と、タイムラインへの自動反映
- 15分刻みの日次タイムラインCSVの出力
- タイムラインへの品番・工程のヒント自動入力
- 本人が確定したタイムラインの取り込み
- 月次の転記用CSV出力
- 出力列の設定ファイルによる定義

### やらないこと（重要）

- **打刻ウィンドウ以外のGUIは作らない。** 確定作業はExcelで行う
- **社内サーバーへの直接書き込みはしない。** 今回はCSV出力まで
- **生ログを本人のPCの外に出さない。** 提出されるのは集計後のCSVのみ
- **他人の記録を閲覧する機能は作らない**
- **AIによるウィンドウタイトルの判定はしない。** 自動化はルールベースのみ
- **リアルタイム表示・ダッシュボードは作らない**
- **勤怠管理には使わない。** 出退勤時刻・休憩時刻の提出は行わない

> 最後の項目は、離席打刻を入れたことで特に重要になりました。「休憩」ボタンの記録は本人のPCに留め、**提出用CSVには一切含めません。** 休憩の長さが管理側に見える設計にすると、ツールの目的が変わってしまいます。

---

## 5. データの3層構造

| 層 | 名前 | 内容 | 置き場所 | 見る人 |
|---|---|---|---|---|
| **L1** | 生ログ | ウィンドウ遷移の記録 ＋ 打刻の記録 | 本人のPCのみ | 本人だけ |
| **L2** | 日次タイムライン | 15分刻み。本人が品番・工程を確定したもの | 本人のPC | 本人だけ |
| **L3** | 転記用CSV | 品番×工程×日 の集計値（作業時間のみ） | 提出 | 管理側 |

**L1はどのような場合も本人のPCから出しません。** 提出されるのはL3のみです。この境界を配布時に必ず説明してください。

L1は上書きせず追記のみとします。抽出ルールや集計ロジックを後から変更した際に、過去分をやり直せるようにするためです。

---

## 6. 離席打刻ウィンドウ

### 見た目と動作

デスクトップの隅に常時表示される、横長の小さなウィンドウ。

```
┌──────────────────────────────────────────────┐
│  25AW-BL-1234                       ▼        │  ← 現在の品番（変更可）
├──────────────────────────────────────────────┤
│  [ 休憩 ] [ トワル組み ] [ 検品 ] [ 打合せ ]  │
└──────────────────────────────────────────────┘
```

| 項目 | 仕様 |
|---|---|
| 表示 | 常時最前面。小さく、閉じずに端に置いておく |
| 操作 | 席を立つ前にボタンを1回クリックするだけ |
| 復帰 | **PC操作を検知した時点で自動的に打刻を終了する。** 戻ったときの操作は不要 |
| 復帰の検知方法 | ウィンドウ自身は判定しない。watcherが書く `state.json` を読んで判断する（→ 第13章） |
| 品番 | 直前にPCで作業していた品番を自動で引き継ぐ。違う場合はプルダウンで最近の品番から選べる |
| 打刻中の表示 | ボタンが押された状態になり、経過時間を表示する |
| 手動終了 | 打刻中のボタンをもう一度押すと終了する（PCの前で作業を切り替えるとき用） |

### 押し忘れへの対応

打刻がないまま一定時間（既定30分）操作が途切れていた場合、**PC操作を再開した時点でウィンドウが問いかけます。**

```
┌──────────────────────────────────────────────┐
│  09:45 〜 10:20 の35分、離席していました       │
│  [ 休憩 ] [ トワル組み ] [ 検品 ] [ 打合せ ]  │
│  [ あとで（日次確定で入力する） ]              │
└──────────────────────────────────────────────┘
```

ここで選べば打刻として記録され、選ばなければ空欄のまま日次確定に回ります。**押し忘れても情報が失われない構造にしておくことが重要です。**

### ボタンの構成は設定で決める

ボタンは固定せず `config.toml` で定義します。ラベル、対応する工程、作業時間に数えるか、品番を引き継ぐかを個別に指定できます。担当業務が違うパタンナーには、設定ファイルを配り分ければ対応できます。

**ボタンは最大6個までを推奨します。** 増やすと選ぶ時間がかかり、押されなくなります。

---

## 7. コマンド仕様

| コマンド | 動作 | 実行者 |
|---|---|---|
| `worklog watch` | PC操作の記録を開始する。Windows起動時に自動実行 | 自動 |
| `worklog punch` | 打刻ウィンドウを表示する。Windows起動時に自動実行 | 自動 |
| `worklog day` | 今日のタイムラインCSVを出力し、Excelで開く | 本人（1日1回） |
| `worklog day --date 2026-08-29` | 指定日のタイムラインを出力（書き忘れた日の遡り用） | 本人 |
| `worklog commit` | 編集済みタイムラインを取り込んで確定する | 本人 |
| `worklog export --month 2026-08` | 転記用CSVを出力する | 本人（月1回） |
| `worklog status` | 未確定の日を一覧表示する | 本人 |

日々の操作は実質 `worklog day` と `worklog commit` の2つだけです。この2つはデスクトップのバッチファイルから起動できるようにし、**パタンナーがコマンドを打たなくて済むようにします。**

---

## 8. ファイル仕様

### 8-1. L1 ウィンドウログ `data/raw/2026-08-30_window.csv`

```csv
start,end,duration_sec,process,window_title,active
2026-08-30T09:02:15,2026-08-30T09:47:30,2715,EXCEL.EXE,25AW-BL-1234_仕様書.xlsx - Excel,true
2026-08-30T09:47:30,2026-08-30T10:31:00,2610,,,false
2026-08-30T10:31:00,2026-08-30T11:52:40,4900,AGMS.exe,25AW-BL-1234 パターン - AGMS,true
```

### 8-2. L1 打刻ログ `data/raw/2026-08-30_punch.csv`

```csv
start,end,label,process,style,ended_by
2026-08-30T09:47:30,2026-08-30T10:31:00,トワル組み,トワル組み,25AW-BL-1234,pc_resume
2026-08-30T14:10:00,2026-08-30T14:55:00,打合せ,会議・打合せ,,button
```

- `ended_by` … `pc_resume`（PC操作で自動終了）/ `button`（手動終了）/ `late_input`（押し忘れの後追い入力）

### 8-2b. 現在状態 `data/state.json`

**watcherが書き、打刻ウィンドウが読むだけのファイルです。** これがあることで、打刻ウィンドウはWindows APIに一切触れずに済みます。

```json
{
  "updated_at": "2026-08-30T10:31:05",
  "active": true,
  "idle_sec": 0,
  "current_style": "25AW-BL-1234",
  "recent_styles": ["25AW-BL-1234", "25AW-SK-0987", "25SS-PT-0455"],
  "last_active_at": "2026-08-30T10:31:05"
}
```

| キー | 意味 | 打刻ウィンドウでの使い道 |
|---|---|---|
| `active` | いま操作されているか | `true` に変わったら打刻を自動終了する |
| `idle_sec` | 何秒操作がないか | 押し忘れの問いかけを出すか判断する |
| `current_style` | 直前にPC作業していた品番 | 打刻時に引き継ぐ |
| `recent_styles` | 最近使った品番 | プルダウンの選択肢にする |

watcherが `poll_interval_sec` ごとに丸ごと書き換えます。打刻ウィンドウは1秒ごとに読み直します。ファイルが無い・壊れている場合、打刻ウィンドウは「品番なし・操作状態不明」として動き続けます（**watcherが落ちても打刻はできる**）。

### 8-3. L2 日次タイムライン `data/daily/2026-08-30_timeline.csv`

**これがパタンナーがExcelで開いて編集するファイルです。**

```csv
時刻,状態,ヒント,品番,工程,メモ
08:30,PC,25AW-BL-1234_仕様書.xlsx,25AW-BL-1234,仕様書,
08:45,PC,25AW-BL-1234_仕様書.xlsx,25AW-BL-1234,仕様書,
09:00,PC,25AW-BL-1234_仕様書.xlsx,25AW-BL-1234,仕様書,
09:45,打刻,トワル組み,25AW-BL-1234,トワル組み,
10:00,打刻,トワル組み,25AW-BL-1234,トワル組み,
10:15,打刻,トワル組み,25AW-BL-1234,トワル組み,
10:30,PC,25AW-BL-1234 パターン - AGMS,25AW-BL-1234,パターン,
12:00,休憩,,,,
13:00,未記録,,,,
```

**状態列（自動）**

| 値 | 意味 | 品番・工程の初期値 |
|---|---|---|
| `PC` | PCを操作していた | ヒントから推定して事前入力 |
| `打刻` | 離席打刻があった | 打刻内容をそのまま入力 |
| `休憩` | 設定した休憩時間帯 | 空欄のまま（集計対象外） |
| `未記録` | 打刻も操作もない | **空欄。ここだけ本人が埋める** |

**優先順位は 打刻 ＞ 休憩設定 ＞ PC操作 ＞ 未記録** です。打刻がある時間帯は、PC操作があってもその打刻を優先します（打合せ中にPCを触っている場合など）。

**本人が実際に手を動かすのは `未記録` の行だけ**になります。同じ品番が続く箇所はExcelの下方向ドラッグでコピーできるため、通常は数行です。

### 8-4. L3 転記用CSV `data/export/2026-08_worklog.csv`

```csv
日付,品番,工程,時間
2026-08-01,25AW-BL-1234,パターン,3.25
2026-08-01,25AW-BL-1234,トワル組み,0.75
2026-08-01,25AW-SK-0987,グレーディング,2.00
```

- 時間は0.25h単位（15分刻みなのでそのまま整合する）
- **`counts_as_work = false` の工程（休憩など）は出力しない**
- 列名・順序・書式は `export_map.toml` で定義する

### 8-5. 設定 `config.toml`

```toml
[schedule]
day_start    = "08:30"
day_end      = "18:00"
slot_minutes = 15

# 休憩時間帯。ここは自動で「休憩」として埋まり、打刻不要
[[schedule.breaks]]
name  = "昼休み"
start = "12:00"
end   = "13:00"

[[schedule.breaks]]
name  = "小休憩"
start = "15:00"
end   = "15:15"

[tracking]
poll_interval_sec  = 5      # 何秒ごとにアクティブウィンドウを見るか
idle_threshold_sec = 180    # 何秒操作がなければ離席とみなすか
ask_after_sec      = 1800   # 打刻なしの離席がこの長さを超えたら復帰時に聞く

# 離席打刻ボタン（最大6個を推奨）
[[buttons]]
label          = "休憩"
process        = "休憩"
counts_as_work = false      # 提出用CSVに含めない
inherit_style  = false      # 品番を引き継がない

[[buttons]]
label          = "トワル組み"
process        = "トワル組み"
counts_as_work = true
inherit_style  = true       # 直前の品番を引き継ぐ

[[buttons]]
label          = "検品・確認"
process        = "検品・確認"
counts_as_work = true
inherit_style  = true

[[buttons]]
label          = "打合せ"
process        = "会議・打合せ"
counts_as_work = true
inherit_style  = false

# 品番の自動抽出ルール（命名規則が判明したら追加していく）
[extract]
style_patterns = [
  '\d{2}[ASFW]{2}-[A-Z]{2}-\d{4}',   # 例: 25AW-BL-1234
]

# ウィンドウから工程を推定するルール
[[process_hints]]
name          = "パターン"
match_process = ["AGMS.exe", "YUKA.exe"]

[[process_hints]]
name          = "仕様書"
match_process = ["EXCEL.EXE"]

[[process_hints]]
name          = "会議・メール"
match_process = ["OUTLOOK.EXE", "Teams.exe", "ms-teams.exe"]

# 工程の選択肢（Excelの入力規則として埋め込む）
[choices]
processes = [
  "パターン", "グレーディング", "修正", "仕様書",
  "トワル組み", "検品・確認", "会議・打合せ", "その他"
]
```

### 8-6. 出力マッピング `export_map.toml`

**社内Excelの列構成が未確認のため、出力形式をコードから外に出しておきます。** 現物が手に入ったら、このファイルだけを書き換えれば対応できます。

```toml
[format]
encoding    = "utf-8-sig"    # Excelで開くためBOM付き
delimiter   = ","
date_format = "%Y/%m/%d"
round_to    = 0.25

[[columns]]
header = "日付"
source = "date"

[[columns]]
header = "品番"
source = "style"

[[columns]]
header = "工程"
source = "process"

[[columns]]
header = "工数(h)"
source = "hours"
```

---

## 9. 品番の自動化を段階的に上げる

命名規則が未確認のため、以下の順で自動化率を上げていきます。**どの段階でもツールは動きます。**

| 段階 | 状態 | 本人の作業 |
|---|---|---|
| 0 | 抽出ルールなし | 品番を全部手入力。打刻で工程は取れているので、埋めるのは品番だけ |
| 1 | 命名規則が判明し、正規表現を1つ追加 | PC作業分は自動。打刻分は引き継ぎで自動。実質確認のみ |
| 2 | 工程の推定ルールを追加 | 埋まっている内容を確認・修正するだけ |

**段階0で配布を始めて構いません。** 使いながら実際のファイル名を見て抽出ルールを足していくのが現実的で、設定ファイルの更新だけで済みます。

---

## 10. 配布と運用

| 項目 | 方式 |
|---|---|
| 配布形式 | PyInstallerで単一exe。インストーラなし |
| 配置 | 各自のPCの任意フォルダ。設定ファイルはexeと同じ場所 |
| 自動起動 | スタートアップフォルダにショートカットを置く |
| 日々の操作 | 打刻ウィンドウのクリック ＋ デスクトップのバッチファイル2つ |
| 設定の配り分け | `config.toml` を担当別に用意して配布 |
| 更新 | 共有フォルダのexeを差し替え、各自がコピーする |
| データの保管 | 各自のPC内。バックアップは本人の責任範囲 |

### 配布時に必ず説明すること

1. **何を記録するか** … PCで開いているウィンドウのタイトル、操作の有無、押した打刻
2. **どこに保存されるか** … 自分のPCの中だけ。生ログも打刻ログも外に出ない
3. **何が提出されるか** … 品番×工程×時間 の集計値のみ。**休憩の記録は含まれない**
4. **止められること** … いつでも記録を停止でき、確定前の内容は自由に編集できる
5. **使わない用途** … 勤怠管理、人事評価には使用しない

**この説明を省略しないでください。** 特に休憩ボタンがあることで勤怠監視と受け取られやすいので、提出データに含まれない点を明示してください。導入前に上長・人事への確認も行ってください。

---

## 11. 完成の判定条件

- [ ] PCを起動すると記録が始まり、打刻ウィンドウが表示される
- [ ] 席を立つ前にボタンを押すと打刻が始まる
- [ ] 戻ってPCを操作すると、打刻が自動で終了する
- [ ] 打刻なしで30分以上離席すると、復帰時に問いかけが出る
- [ ] 設定した休憩時間帯が、タイムライン上で自動的に「休憩」になる
- [ ] `worklog day` でタイムラインCSVがExcelで開く
- [ ] PC操作・打刻があった時間帯が事前に埋まっており、`未記録` だけが空欄になっている
- [ ] 品番・工程を入力して保存し、`worklog commit` で確定できる
- [ ] 未確定の日が `worklog status` で分かる
- [ ] `worklog export --month` で転記用CSVが出る。**休憩は含まれない**
- [ ] 出力の列名を `export_map.toml` で変更でき、コード変更が不要
- [ ] ボタン構成・勤務時間・休憩時間を `config.toml` で変更できる
- [ ] 生ログが `data/raw/` の外に出ない
- [ ] 記録を止めても、それまでの記録が壊れない
- [ ] **パタンナー1人に1日試してもらい、確定作業が5分以内に終わる**

最後の1項目が本当の合格条件です。他が全部できていても、確定作業が10分かかるなら使われません。

---

## 12. 導入前に確認したいこと

| 確認項目 | 誰に | なぜ必要か |
|---|---|---|
| ファイル名に品番が入っているか。命名規則は | パタンナー1人に5分 | 自動抽出ルールを書けるか決まる |
| 使用しているCADソフトの実行ファイル名 | 同上 | 工程の推定ルールに使う |
| **PC外作業の種類。ボタンにすべきものは何か** | 同上 | ボタン構成を決める。多すぎると押されない |
| 始業・終業・休憩の時刻。個人差はあるか | 同上 | 設定を配り分ける必要があるか決まる |
| 社内Excelの列構成・単位・丸め方 | 工数を集計している人 | `export_map.toml` を書ける |
| 工程の区分は何種類あるか | 同上 | 選択肢を確定できる |
| PCの持ち出し・複数台使用の有無 | — | 記録の統合が必要か決まる |

**上の3つはパタンナー1人に10分聞けば分かります。** 着手前に確認することを勧めます。

---

## 13. 実装の進め方（研修での分担）

### ゼロイチは1回、改造は何回でも

GUIで難しいのは**作ること**であって、**直すこと**ではありません。目の前に動くウィンドウがあれば「ボタンを大きく」「窓を右下に固定して」と言うだけで直せますし、結果が目で見えるぶん確認はCLIより速く済みます。

この設計書があれば、最初のウィンドウは**1つのプロンプトで出せます。** そこから先の改造を厚くします。

| 段階 | 内容 | 回数 |
|---|---|---|
| ゼロイチ | 設計書を渡して打刻ウィンドウを1つ作らせる | 1回 |
| 改造 | 設定対応、位置記憶、プルダウン、経過時間、押し忘れ問いかけ | 何回でも |

**配布後に一番よく直すことになるのはGUIです。** ボタンのラベル、窓の大きさ、位置、最前面にするかどうか。現場から必ず要望が来る場所なので、ここを自分で直せる状態にしておくことが、ツールを生かし続ける条件になります。

### GUIとCLIの接点

**接点は2ファイルだけ、しかも一方向です。** 片方を壊しても、もう片方は動き続けます。

```
┌──────────────────┐                          ┌──────────────────┐
│  打刻ウィンドウ    │ ──書く──▶ punch.csv ──読む──▶ │   watcher / 本体   │
│  （tkinterのみ）   │                          │  （Windows API）  │
│                  │ ◀──読む── state.json ◀──書く── │                  │
└──────────────────┘                          └──────────────────┘
```

| ファイル | 書く側 | 読む側 |
|---|---|---|
| `data/raw/YYYY-MM-DD_punch.csv` | 打刻ウィンドウ | 本体 |
| `data/state.json` | watcher | 打刻ウィンドウ |
| `config.toml` | 人間 | 両方 |

**打刻ウィンドウはWindows APIに一切触りません。** 離席の判定はwatcherが行い、結果を `state.json` に書きます。この分け方には実務上の利点が4つあります。

| 効果 | 内容 |
|---|---|
| GUIがWindows依存でなくなる | 純粋なtkinterだけで動くため、表示の確認が速い |
| 片方が壊っても片方は動く | watcherと打刻ウィンドウは別プロセス |
| watcherが落ちても打刻できる | `state.json` が読めなければ「品番なし」として継続する |
| 本体のテストにGUIが要らない | `punch.csv` を手で数行書けばタイムライン生成を確認できる |

`worklog watch` と `worklog punch` は別コマンドとし、スタートアップから両方起動します。

### 改造課題（難易度順）

| 順 | 課題 | 学べる設計原則 |
|---|---|---|
| 1 | ボタン構成を `config.toml` から読む | 設定をコードから追い出す |
| 2 | 最前面表示、窓の位置とサイズを記憶する | 現場の使い勝手要望への対応 |
| 3 | 品番プルダウンを `state.json` から読む | 部品どうしの接点を守る |
| 4 | 打刻中に経過時間を表示する | 状態を持つUIの扱い |
| 5 | 押し忘れの問いかけを出す | 仕様の穴を埋める |
| 6 | ボタンの並び順・色を設定で変える | 要望対応の反復 |

**1課題ごとにコミットします。** GUIは好みで行ったり来たりするため、「やっぱり前のほうが良かった」が実際に起きます。gitの価値が一番わかりやすく体感できる場面です。

### 実装上の注意

**tkinterは例外が黙って消えることがあります。** ボタンを押しても何も起きず、エラーも表示されない状態になりやすい。**最初のプロンプトの時点で「例外は必ずログファイルに書く」と指示してください。** これを忘れると、原因調査で長時間詰まります。

### 当日の到達目標

**完成ではなく、ループを回せるようになることを目標とします。**

- 設計書がある
- gitで戻せる
- 詰まったときの聞き方を知っている
- ゼロイチと改造を1回ずつ通した

この状態になっていれば、残りは各自で続けられます。研修中に完成させてしまうと、その後自力で拡張する経験が抜け落ちます。

| 範囲 | 当日 | 持ち帰り |
|---|---|---|
| watcher と `state.json` | ○ | |
| 打刻ウィンドウ（ゼロイチ＋改造2〜3） | ○ | |
| 打刻ウィンドウ（改造4〜6） | | ○ |
| `worklog day` / `commit` / `export` | | ○ |
| exe化と配布 | | ○ |
